CREATE TABLE user_info (
    user_id INT PRIMARY KEY,    -- Unique identifier for the user
    age INT,                    -- Age of the user
    gender VARCHAR(10),         -- Gender (e.g., M, F)
    location VARCHAR(50)        -- Location (e.g., Khonkaen, Chonburi)
);